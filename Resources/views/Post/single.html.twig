{% extends 'MayecoPostBundle::twbslayout.html.twig' %}

{%- block body -%}

    <h1>{{ post.titulo }}</h1>
    <p>{{ post.texto }}</p>

    {% include "MayecoPostBundle:Post:comments.html.twig" with { comments: comments, id: post.id } %}

    <h4><a href="{{ path('mayeco_post_list') }}">Regresar</a></h4>

{%- endblock -%}
